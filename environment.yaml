name: monitor
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python=3.10.*
  - pytest
  - pandas
  - pyyaml
  - numpy
  - matplotlib
  - pre-commit
  - google-auth
  - pip
  - pydantic<2.0
  - git_root
  - pip:
      - orgparse
      # This package is available via conda forge but sadly only
      # a very old version thereof.
      - google-api-python-client==2.48.0
      - python-telegram-bot
